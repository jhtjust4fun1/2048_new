# Agent 强制开发工作流

本项目为 Cocos Creator 3.8.8 + TypeScript 游戏项目。

执行任何代码修改任务时，必须严格按照以下工作流执行，不得跳过关键步骤。

---

## Step 1：理解需求

首先分析用户需求，明确：

* 要新增什么功能
* 要修改什么功能
* 涉及哪些游戏逻辑
* 涉及哪些 UI
* 是否涉及 Scene / Prefab
* 是否涉及资源
* 是否涉及网络、存档、数据等

不要立即修改代码。

如果需求存在歧义，优先基于现有代码推断，不要擅自扩大需求范围。

---

## Step 2：定位相关代码

根据需求搜索项目代码，找到：

* 相关 Scene
* 相关 Prefab
* 相关 Component
* 相关 TypeScript 文件
* 相关方法
* 相关变量
* 相关事件
* 相关资源
* 相关调用方

必须优先通过代码搜索确认实际实现位置。

禁止仅根据文件名猜测代码结构。

---

## Step 3：阅读现有实现

找到相关文件后，必须先阅读现有代码。

重点分析：

```text
类结构
↓
生命周期
↓
核心方法
↓
数据流
↓
事件流
↓
调用关系
↓
UI / Prefab 关系
```

必须理解现有代码后才能修改。

### 特别注意

Cocos Creator 项目中的：

```text
@property
onLoad
start
update
onEnable
onDisable
onDestroy
Node
Prefab
Scene
Component
Event
Tween
Animation
```

可能存在隐式依赖。

不要仅修改当前文件而忽略这些关系。

---

# Step 4：分析影响范围

修改前必须回答：

```text
这个功能当前在哪里实现？

哪些代码会被修改？

哪些代码调用它？

哪些代码依赖它？

修改后可能影响哪些已有功能？
```

对于公共方法、Manager、Singleton、BaseClass、Utils 等核心代码，需要更加谨慎。

---

# Step 5：制定最小修改方案

在真正修改之前，选择最小改动方案。

优先级：

```text
方案 1：复用已有代码
        ↓
方案 2：扩展已有方法
        ↓
方案 3：新增一个局部方法
        ↓
方案 4：新增独立 Component
        ↓
方案 5：新增独立模块
        ↓
方案 6：修改架构 / 重构
```

默认必须选择前面的方案。

只有确认前面的方案无法实现需求时，才能使用后面的方案。

---

# Step 6：执行最小代码修改

开始修改后必须遵循：

```text
Minimal Change
Minimal Diff
Minimal Risk
```

也就是说：

> 能增加 10 行代码解决的问题，不要修改 100 行代码。

例如原代码：

```ts
updatePlayer() {
    this.updatePosition();
    this.updateAnimation();
}
```

现在需要增加一个音效。

应该：

```ts
updatePlayer() {
    this.updatePosition();
    this.updateAnimation();

    this.playSound();
}
```

而不是重写整个：

```ts
updatePlayer()
```

---

# Step 7：禁止无关修改

执行任务过程中，如果发现以下问题：

```text
代码风格不好
命名不好
存在 any
代码可以优化
存在旧代码
存在 TODO
存在 console.log
存在潜在性能问题
```

如果与当前需求无关：

**不要修改。**

只处理当前任务。

---

# Step 8：禁止删除和清空

任何情况下：

### 禁止

```text
清空已有文件
删除整个类
删除已有方法
删除已有功能
删除未知用途代码
删除未知用途资源
删除 Prefab 节点
删除 Scene 节点
```

除非用户明确要求删除。

---

# Step 9：保护 Cocos Creator 序列化数据

修改：

```ts
@property(...)
```

时必须特别谨慎。

禁止无必要修改：

```text
@property(Node)
@property(Sprite)
@property(Label)
@property(Prefab)
@property(Animation)
```

以及属性名称和类型。

因为这些属性可能已经被：

```text
Scene
Prefab
Inspector
```

引用。

---

# Step 10：修改完成后检查 Diff

代码修改完成后必须检查本次修改范围。

检查：

```text
修改了哪些文件？
每个文件修改了什么？
是否出现大量无关修改？
是否出现大面积删除？
是否出现格式化整个文件？
是否修改了与需求无关的代码？
```

如果发现：

```text
大面积 Diff
```

必须重新检查，并尽量恢复无关修改。

---

# Step 11：检查原有功能

必须主动检查新增代码是否可能影响：

```text
原有游戏逻辑
原有 UI
原有事件
原有生命周期
原有数据
原有 Prefab
原有 Scene
原有资源
```

重点确认：

> 新增功能是“增加”，而不是“替换”原有功能。

---

# Step 12：TypeScript 检查

完成修改后检查：

```text
import
类型
变量
方法
参数
返回值
null / undefined
```

确认没有明显 TypeScript 错误。

如果项目存在现有类型检查、Lint 或构建命令，可以执行对应检查。

---

# Step 13：Cocos Creator 验证

如果环境允许，应进行：

```text
TypeScript 编译
↓
项目构建
↓
运行项目
↓
验证新增功能
↓
验证原有功能
```

如果无法运行 Cocos Creator：

不要假装已经运行测试。

明确说明：

```text
未执行 Cocos Creator 实际运行验证，仅完成代码级检查。
```

---

# Step 14：最终结果检查

任务完成前必须进行最后检查：

```text
□ 需求是否完成
□ 是否修改了最少的代码
□ 是否存在无关修改
□ 是否删除了代码
□ 是否清空了文件
□ 是否影响原有功能
□ 是否修改了不必要的 Prefab / Scene
□ 是否破坏 @property
□ 是否产生 TypeScript 错误
□ 是否完成必要的构建 / 测试
```

---

# Step 15：最终输出

完成后只需要简洁汇报：

```text
实现内容：
- xxx
- xxx

修改文件：
- xxx.ts
- xxx.ts

修改策略：
- 基于现有代码增量修改
- 未进行无关重构
- 未删除原有功能

验证：
- TypeScript：通过 / 未执行
- Build：通过 / 未执行
- Cocos 运行验证：通过 / 未执行
```

---

# 强制原则

任何任务都必须遵守：

### 1. 不要直接重写文件

先读：

```text
Read → Understand → Modify
```

禁止：

```text
Guess → Rewrite
```

### 2. 不要为了新增功能而重构

```text
Feature Request
      ↓
Minimal Change
      ↓
完成
```

而不是：

```text
Feature Request
      ↓
Refactor
      ↓
Rewrite
      ↓
大量修改
```

### 3. 不确定就保留

```text
不确定能不能删除
→ 不删除

不确定能不能修改
→ 不修改

不确定是否影响其他功能
→ 先分析

不确定资源是否使用
→ 不删除
```

### 4. 默认保护现有代码

已有代码默认视为：

```text
有效代码
+
正在使用
+
可能存在隐藏依赖
```

除非有明确证据证明可以修改或删除。

---

# Agent 执行模型

每次任务必须遵循：

```text
┌─────────────────────┐
│      用户需求        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      理解需求        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      搜索代码        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      阅读代码        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      分析依赖        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│    制定最小方案      │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│     最小代码修改     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      检查 Diff       │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│    TypeScript 检查   │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│   Cocos 功能验证     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│      汇报结果        │
└─────────────────────┘
```

**以上步骤为强制工作流。**

除非用户明确要求，否则 Agent 不得跳过“阅读代码、影响分析、最小方案、Diff 检查”。
